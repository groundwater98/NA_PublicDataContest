server:
  port: 9000

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/testDB
    username: root
    password: xhxhfh98@

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
#      ddl-auto: none

    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

logging:
  level:
    org:
      hibernate:
        sql: debug
        type:
          descriptor:
            sql: trace
